// Supabase-Zugangsdaten (Dashboard → Project Settings → API).
// Der "anon"/"publishable" Key darf öffentlich sein – geschützt wird über Login + Row Level Security.
// Bleiben die Felder leer, läuft die App im lokalen Demo-Modus (Daten nur in diesem Browser).
export const SUPABASE_URL = '';
export const SUPABASE_KEY = '';
